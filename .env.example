# Compose only: choose a new long URL-safe random password.
POSTGRES_PASSWORD=replace_with_a_long_random_url_safe_password
# For PowerShell local mode set $env:NEXUS_... explicitly. Local CLI does not auto-load .env.
# NEXUS_DATABASE_URL=sqlite:///./data/nexus-v1.db
# NEXUS_MANIFEST=config/projects.json
# NEXUS_REDIS_URL=redis://127.0.0.1:6379/0
# NEXUS_ALLOWED_ORIGINS=https://your-redpa-service.example
# NEXUS_ALLOW_HTTP=false
# NEXUS_MODEL_URL=https://your-model-service.example/v1/chat/completions
# NEXUS_MODEL_NAME=your-configured-model
# NEXUS_MODEL_API_KEY=never_commit_real_keys
# NEXUS_EMBEDDING_URL=https://your-embedding-service.example/v1/embeddings
# NEXUS_EMBEDDING_MODEL=your-configured-embedding-model
# NEXUS_EMBEDDING_API_KEY=never_commit_real_keys
# NEXUS_CREDENTIAL_FILE=/absolute/path/outside/repository/credentials.json
